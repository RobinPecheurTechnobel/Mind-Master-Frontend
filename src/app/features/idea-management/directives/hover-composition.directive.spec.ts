import { HoverCompositionDirective } from './hover-composition.directive';

describe('HoverCompositionDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverCompositionDirective();
    expect(directive).toBeTruthy();
  });
});
